var addedComment = $("<%= j(render @comment) %>");
addedComment.hide();

$("#<%= dom_id(@comment.photo) %>_new_comment_form").before(addedComment);

addedComment.slideDown();

$("#<%= dom_id(@comment.photo) %>_new_comment_form #comment_body").val("");

<%= render 'shared/toast' %>

